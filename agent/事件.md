
# SlimefunHelper 事件监听通道文档

## Event 类说明

所有事件回调都接收 `Event<T>` 对象，该类提供以下核心方法：

| 方法 | 说明 |
|------|------|
| `T context()` | 获取事件主体（如攻击事件中的 `HitResult`） |
| `Event<T> context(T val)` | 修改事件主体（需要 `canModifyContext` 为 true） |
| `boolean isCancelled()` | 检查事件是否已被取消 |
| `Event<T> cancel()` | 取消事件（需要 `canCancel` 为 true） |
| `<W> W getArgs(int idx)` | 获取额外参数（按声明顺序索引） |
| `boolean canCancel()` | 是否允许取消 |
| `boolean canModifyContext()` | 是否允许修改主体 |

> **重要约定**：同一 `EventChannel` 的 `canCancel` 和 `canModifyContext` 属性是**固定的**，已在通道定义处的注解（如 `@Cancelable`、`@Modifiable`）中明确声明。开发者在处理事件时**不应修改这些标志**，也**无需显式调用 `canCancel()` 或 `canModifyContext()` 进行判断**，除非需要根据是否允许取消/修改来执行不同的分支逻辑（例如只在可取消时尝试取消）。通常情况下，您可以直接调用 `cancel()` 或 `context(val)`，如果通道不允许该操作，调用会抛出异常（由框架保证）。但为了代码健壮性，在不确定通道定义时可以先调用对应的 `canXxx()` 方法检查。

**使用示例**：

```java
private void onGameRender(Event<GameRenderer> event) {
DeltaTracker tickCounter = event.getArgs(0);
boolean isRenderWorld = event.getArgs(1);
// 该通道被标记为 @Cancelable，所以可以直接取消
if (someCondition) {
event.cancel();
}
}

private void onChatSend(Event<String> event) {
// 该通道被标记为 @Modifiable，所以可以直接修改
if (event.canModifyContext()) { // 可选检查，但通常直接调用即可
event.context("[Prefix] " + event.context());
}
}
```

## 事件通道访问规范

**所有事件通道（包括 `Listener` 和 `RenderListener` 中的通道）都必须通过对应的 `getter` 方法访问**，禁止直接访问静态字段。

| 类 | 正确方式 | 错误方式 |
|---|----------|----------|
| `Listener` | `Listener.getPostGameTick()` | `Listener.postGameTick` |
| `Listener` | `Listener.getPacketPoint()` | `Listener.packetPoint` |
| `RenderListener` | `RenderListener.getRenderLayerTasks()` | `RenderListener.renderLayerTasks` |

**注册示例**：
```java
// 基础事件
registerListener(Listener.getPostGameTick(), this::onTick);
registerListener(RenderListener.getRenderLayerTasks(), this::onRender);

// 精确网络包事件（避免使用 getPacketSendPoint/getPacketAcceptPoint）
registerListener(Listener.getPacketPoint().getChannel(ServerboundUseItemOnPacket.class), this::onInteractBlock);
```

---

## 基础游戏事件

| 通道 | 类型 | 特性 | 说明 |
|------|------|------|------|
| `gameJoinPoint` | `LocalPlayer` | Broadcast | 玩家加入游戏（世界加载完成） |
| `worldSwitchPoint` | `Level` | Broadcast | 切换世界 |
| `serverDisconnectPoint` | `Void` | Broadcast | 断开与服务器的连接 |
| `preTick` | `Void` | Cancelable, Broadcast | 每 tick 开始前 |
| `postTick` | `Void` | Broadcast | 每 tick 结束后 |
| `preGameTick` | `LocalPlayer` | Broadcast | 游戏 tick 开始（玩家存在） |
| `postGameTick` | `LocalPlayer` | Broadcast | 游戏 tick 结束（玩家存在） |
| `gameRender` | `GameRenderer` | Cancelable, ExtraArgs: `DeltaTracker, boolean` | 游戏渲染时 |
| `languageReload` | `Language` | Experimental, Broadcast | 语言重新加载 |
| `clientMainExit` | `Minecraft` | Cancelable(optional) | 客户端退出 |

## 聊天事件

| 通道 | 类型 | 特性 | 说明 |
|------|------|------|------|
| `chatSend` | `String` | Cancelable, Modifiable | 发送聊天消息（可修改内容） |
| `messageAddToHud` | `Component` | Cancelable, Modifiable, ExtraArgs: `MessageSignature, ChatType.Bound` | 添加消息到 HUD |
| `messageAddToVisible` | `GuiMessage` | Cancelable, Modifiable | 添加消息到可见聊天区域 |
| `chatScreenSendMessage` | `String` | Cancelable, Modifiable | 在聊天屏幕发送消息 |

## 屏幕事件

| 通道 | 类型 | 特性 | 说明 |
|------|------|------|------|
| `postCloseScreen` | `Screen` | Broadcast | 关闭屏幕后 |
| `preSetScreen` | `Screen` | Cancelable, Modifiable | 设置屏幕前 |
| `postSetScreen` | `Screen` | Cancelable | 设置屏幕后（取消会阻止后续操作） |
| `postOpenHandledScreen` | `AbstractContainerScreen<?>` | Broadcast | 打开容器屏幕后 |
| `postInitializeScreen` | `Screen` | Broadcast | 屏幕初始化后 |
| `postToggleRecipeBook` | `RecipeBookMenu` | Broadcast, ExtraArgs: `RecipeBookComponent, Button` | 切换配方书后 |
| `clickCraftingRecipe` | `NetworkRecipeId` | Broadcast | 点击合成配方 |

## 网络包事件

| 通道 | 类型 | 特性 | 获取方法 |
|------|------|------|----------|
| `packetPoint` | `PacketEventChannel` | Cancelable, Modifiable, ExtraArgs: `Connection`, Dispatch by "type and side" | `getPacketPoint()` |
| `packetPreHandlePoint` | `PacketEventChannel` | Cancelable, ExtraArgs: `PacketListener`, Dispatch by "type" | `getPacketPreHandlePoint()` |
| `packetPostHandlePoint` | `PacketEventChannel` | Broadcast, ExtraArgs: `PacketListener`, Dispatch by "type" | `getPacketPostHandlePoint()` |
| `packetPostSendPoint` | `PacketEventChannel` | ExtraArgs: `Connection` | `getPacketPostSendPoint()` |
| `packetAcceptPoint` | `EventChannel<Packet<?>>` | Cancelable, ExtraArgs: `Connection` | `getPacketAcceptPoint()` |
| `packetSendPoint` | `EventChannel<Packet<?>>` | Cancelable, ExtraArgs: `Connection` | `getPacketSendPoint()` |

**推荐使用精确通道**：`getPacketPoint().getChannel(XXX.class)` 等，避免使用 `getPacketSendPoint` / `getPacketAcceptPoint`。

## 玩家行为事件

| 通道 | 类型 | 特性 | 说明 |
|------|------|------|------|
| `clientPlayerSendMovementPoint` | `LocalPlayer` | Cancelable | 发送移动数据包前 |
| `entityTrackDataUpdate` | `EventChannelDispatcher<SynchedEntityData.DataValue<?>>` | Cancelable, Modifiable, Experimental, ExtraArgs: `Entity`, Dispatch by `Entity.getType` | 实体跟踪数据更新 |
| `thisPlayerSpawnPoint` | `LocalPlayer` | Broadcast | 玩家重生点设置 |
| `playerNotFlyJumpPoint` | `Integer` | Cancelable, Modifiable | 非飞行模式下的跳跃（参数：跳跃次数） |
| `teleportConfirmResponsePoint` | `MovTasks.MovInfo` | Modifiable | 传送确认响应 |
| `useItemCooldownReset` | `Integer` | Cancelable, Modifiable | 物品冷却重置 |
| `playerVelocityTick` | `Vec3` | Cancelable, Modifiable | 玩家速度每 tick 更新 |
| `playerKeyboardInputTick` | `Input` | Broadcast | 键盘输入 tick |
| `playerInitConfiguration` | `LocalPlayer` | Broadcast | 玩家初始化配置 |
| `playerFallFlyingTick` | `Integer` | Modifiable, Cancelable | 鞘翅飞行 tick |
| `playerSwitchFallFlying` | `Boolean` | Modifiable, Cancelable, ExtraArgs: `currentFallFlying` (Boolean) | 切换鞘翅飞行状态 |
| `playerTravelingTick` | `Vec3` | Cancelable | 玩家移动 tick |
| `playerChangeLook` | `FPoint` | Cancelable, Modifiable | 玩家视角变化 |

## 实体事件

| 通道 | 类型 | 特性 | 说明 |
|------|------|------|------|
| `otherPlayerJoinPoint` | `PlayerInfo` | Broadcast | 其他玩家加入 |
| `otherPlayerExitPoint` | `PlayerInfo` | Broadcast | 其他玩家退出 |
| `entityClientVelocityUpdate` | `Vec3` | Cancelable, Modifiable, ExtraArgs: `Entity` | 实体客户端速度更新 |
| `entityPreTickListener` | `Entity` | Cancelable | 实体 tick 前 |
| `entityMidTickListener` | `Entity` | Broadcast | 实体 tick 中 |
| `entityPostTickListener` | `Entity` | Broadcast | 实体 tick 后 |
| `entityDataListener` | `Entity` | Broadcast, ExtraArgs: `updatedEntry` (List) | 实体数据更新 |
| `entityCreateListener` | `Entity` | Cancelable, Modifiable, ExtraArgs: `EntityType` | 实体创建 |
| `entityRemoveListener` | `Entity` | Broadcast, ExtraArgs: `RemovalReason` | 实体移除 |

## 世界与方块实体事件

| 通道 | 类型 | 特性 | 说明 |
|------|------|------|------|
| `blockEntityTickListener` | `BlockEntityTickInvoker` | Cancelable | 方块实体 tick |

## 交互与攻击事件

| 通道 | 类型 | 特性 | 说明 |
|------|------|------|------|
| `preHandleInputEvents` | `Void` | Cancelable | 处理输入前 |
| `postHandleInputEvents` | `Void` | Broadcast | 处理输入后 |
| `prePlayerUseItemAtBlock` | `BlockHitResult` | Cancelable | 在方块上使用物品前 |
| `postPlayerUseItemAtBlock` | `BlockHitResult` | Broadcast | 在方块上使用物品后 |
| `mineBlockAction` | `HitResult` | Cancelable, Modifiable | 挖掘方块动作 |
| `attackAction` | `HitResult` | Cancelable, Modifiable | 攻击动作 |
| `itemUseAction` | `HitResult` | Cancelable, Modifiable, ExtraArgs: `Hand` | 使用物品动作 |

## 输入与窗口事件

| 通道 | 类型 | 特性 | 说明 |
|------|------|------|------|
| `resolutionChange` | `Point` | Broadcast | 窗口分辨率改变 |
| `KeyboardInput` | `Keyboard` | Cancelable, ExtraArgs: `keyCode, scanCode, action, modifiers` | 键盘输入 |
| `mouseButton` | `Mouse` | Cancelable, ExtraArgs: `eventButton, action, mode` | 鼠标按键 |
| `mouseScroll` | `Mouse` | Cancelable, ExtraArgs: `horizontal, vertical` | 鼠标滚动 |
| `mouseMove` | `Mouse` | Cancelable, ExtraArgs: `mouseX, mouseY` | 鼠标移动 |
| `mouseDrag` | `Mouse` | Cancelable, ExtraArgs: `mouseX, mouseY, deltaX, deltaY` | 鼠标拖动 |
| `charTyped` | `Character` | Cancelable, ExtraArgs: `codepoint, modifiers` | 字符输入 |
| `hotKeyTriggeredListener` | `IHotKey` | Cancelable, ExtraArgs: `IInputManager` | 热键触发 |

## 异常事件

| 通道 | 类型 | 特性 | 说明 |
|------|------|------|------|
| `exceptionListener` | `EventChannelDispatcher<WrapperException>` | Cancelable, Dispatch by `WrapperException.type` | 异常捕获 |

## 自定义事件

| 通道 | 类型 | 特性 | 说明 |
|------|------|------|------|
| `customListener` | `EventChannelDispatcher<EventContainer<?>>` | Cancelable(optional), Modifiable(optional), Dispatch by `EventContainer.getType` | 自定义事件分发 |

## 网络连接事件

| 通道 | 类型 | 特性 | 说明 |
|------|------|------|------|
| `connectionChannelInitialize` | `ChannelPipeline` | Broadcast, ExtraArgs: `PacketFlow, Boolean` | 连接通道初始化 |

## 渲染事件（RenderListener）

| 通道 | 类型 | 特性 | 说明 |
|------|------|------|------|
| `itemDataOverrideForModel` | `ItemStack` | Modifiable, Cancelable | 物品模型数据覆盖 |
| `customModelOverride` | `Identifier` | Modifiable, Cancelable, ExtraArgs: `originalItemStack` | 自定义模型覆盖 |
| `detachedItemStackInformation` | `ItemStack` | Modifiable, Cancelable, ExtraArgs: `originItemStack` | 获取物品额外信息 |
| `renderLayerTasks` | `MatrixStack` | Broadcast, ExtraArgs: `ticksDelta` | 渲染层任务（世界渲染） |
| `renderSlot` | `GuiGraphicsExtractor` | Broadcast, ExtraArgs: `AbstractContainerScreen, Slot` | 渲染屏幕中的槽位 |
| `renderHandledScreen` | `GuiGraphicsExtractor` | Broadcast, ExtraArgs: `AbstractContainerScreen, mouseX, mouseY, delta` | 渲染容器屏幕 |
| `resourceReload` | `ResourceManager` | Broadcast | 资源重新加载 |
| `asyncItemModelSupply` | `Set<Identifier>` | Broadcast, ExtraArgs: `ResourceManager` | 异步物品模型供应 |
| `atlasSourceSupply` | `Set<Identifier>` | Broadcast, ExtraArgs: `ResourceManager, Identifier` | 图集源供应 |
| `tooltipShow` | `List<Component>` | Broadcast, ExtraArgs: `ItemStack, advance, creative` | 显示物品提示框 |
| `entityRenderListener` | `Entity` | Cancelable | 实体渲染 |
| `blockEntityRenderListener` | `BlockEntity` | Cancelable | 方块实体渲染 |
| `particleRenderListener` | `Particle` | Cancelable | 粒子渲染 |

## 使用示例

```java
// 在 BaseModule 子类中
@Override
public void registerAll() {
super.registerAll();
// 监听后游戏 tick
registerListener(Listener.getPostGameTick(), this::onPostGameTick);
// 监听攻击动作（高优先级）
registerListener(Listener.getAttackAction(), this::onAttack, -999);
// 精确监听特定包
registerListener(Listener.getPacketPoint().getChannel(ServerboundUseItemOnPacket.class), this::onInteractBlock);
}
```
